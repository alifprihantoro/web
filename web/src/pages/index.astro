---
import { tittle, description, username, image, type, icon } from '../data.json'
import Pages from '@ui/home'
import Layout from '../layouts.astro'
---

<Layout
	tittle={tittle}
	description={description}
	username={username}
	image={image}
	type={type}
	icon={icon}
	Pages={Pages}
/>
