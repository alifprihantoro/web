---
import configs from 'configs/data/home/pages'
import Pages from '@ui/home'
import Layout from '../layouts.astro'
const { tittle, description, username, image, type, icon } = configs
import '../dev.css'
---

<Layout
  tittle={tittle}
  description={description}
  username={username}
  image={image}
  type={type}
  icon={icon}
  Pages={Pages}
/>